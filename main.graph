pow :: (value: int, power: int) -> int {
    if power == 0 {
        <- 1;
    }

    i: int = 0;
    total: int = value;
    while i < power {
       total = total * value;
    }
    <- total;
}

main :: (){
}

